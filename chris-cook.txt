code pen link : https://codepen.io/CookChris98/pen/XWgoLrV?editors=1100


 1. What is Semantic HTML? 
        A markup language that uses elememts that usually have a spefiic mean/purpose.
   
    2. What is HTML used for? 
        It is used to give content on a webpage a meaning or specifc spots and funictions.

    3. What is an attribute and where do we put it? 
        It is almost like a flag in side of a tag that, ex. <a href="abcd">

    4. What is the h1 tag used for? How many times should I use it on a page?
        It is used for the main title of the webpage. It should only be used one 

    5. Name two tags that have required attributes
        The <img> tag, and the <a> tag.

    6. What do we put in the head of our HTML document? 
            the head, html and body tags
    7. What is an id? 
        it is an attribute that has a specific value.

    8. What elements can I add an id to? 

    9. How many times can I use the same id on a page? 
        You can use the same id once

    10. What is a class? 
            they are titles that can appear on many different elememts
    11. What elements can I add a class to? 
        section and div
    12. How many times can I use the same class on a page? 
            once 
    13. How do I get my link to open in a new tab?
            you would use target= "_blank"
    14. What is the alt attribute in the image tag used for? 
                it is used to add a description to said image on the web page
    15. How do I reference an id?
            use a #
    16. What is the difference between a section and a div
            usually in a section the content that is inside is grouped and div really just adds space.
    17. What is CSS used for? 
            it is used to add style to the page
    18. How to we select an element? Example - every h2 on the page
            for the h2 would would just do h2 then add styles in currly brackets
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
            the id calls to a specific thing, while a class can be anything with a class of said value. 
            for example if you wanted to select a group of things youd use class. if you were targeting just one thing youd use an id.
    20. How do we select classes in CSS?
             We use a "."
    21. How do we select a p element with a single class of “human””?
            .human p
    22. What is a parent child selector? When would this be useful? 
            It selects all of the elements that are a direct child of said parent element.

    23. How do you select all links within a div with the class of sidebar?
            .sidebar
    24. What is a pseudo selector?
            used to add style to a specific element ex. h2:hover

    25. What do we use the change the spacing between lines?
            We would use line-height:5px;
    26. What do we use to change the spacing between letters?
                you would use letter-spacing: 5rem;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
                you would use text transform the either capitalize, uppercase, lowercase
    28. How do I add a 1px border around my div that is dotted and black?
            border: 15px dotted black;
    29. How do I select everything on the page? 
            body
    30. How do I write a comment in CSS?
            you would use the command / on a mac 
    31. How do I find out what file I am in, when I am using the command line? 
                you would type "pwd"
    32. Using the command line - how do I see a list of files/folders in my current folder?
                you would type "ls"
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
                you would use git rm but it could delete the entire project and not just your branch.
    34. Why should I use version control? 
                It helps us and other devs in the future know what changes were made and why/
    35. How often should I commit to github?
                You should commit every 20min or so 
    36. What is the command we would use to push our repo up to github?
                 git push -u origin first-last (branch name)
    37. Walk me through Lambda's git flow. 
            fork the repository 
            copy the link in in the green code button (make sure its SSH)
            in the terminal cd to where you want to put the file 
            then git clone file link
            cd into the new repository
            use git checkout -b first-last to create a new branch 
            then  code . to work
            then you would do the git add . then git commit -m "commit message"
            then once you are finished to push back to github you would do git push -u origin branch-name 
            create a pull request and hand in the link to the pull request 